# Frontend Environment Variables
# Copy to .env.local and fill in

# Local development (Vite proxy handles /api → localhost:3001)
VITE_API_URL=/api

# Production — point directly to your Render/Railway backend
# VITE_API_URL=https://rms-api-YOURNAME.onrender.com/api
